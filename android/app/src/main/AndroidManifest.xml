<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
        All files access. It is the whole difference between a cleaner that can
        see the device and one that can only see its own cache: without it the
        app is confined to `getCacheDir()` and whatever folder the user hands
        over through the picker.

        Play requires a declaration for this one, under the file manager use
        case. A build that is refused it still runs — `AndroidStorageAccessRepo`
        reports `StorageAccessLevel.appOnly` and the screen says what that means
        instead of scanning an empty device and calling it clean.

        `/Android/data` and `/Android/obb` stay unreadable even with it granted.
        No permission exists that opens them to a normal app, so no rule names
        another app's cache.
    -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <!--
        The pre-scoped-storage route, for devices below API 30 where the
        permission above does not exist. Capped rather than open ended: past
        those versions the system ignores them, and an uncapped declaration is
        a Play review question with no upside.
    -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <application
        android:label="Storage Cleaner"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <!--
            `screenOrientation` is upright, and says so here as well as in
            `main.dart`. Dart cannot run before the engine does, so without the
            attribute the launch theme appears sideways on a phone held that way
            and then snaps upright. `configChanges` already lists `orientation`
            and `screenSize`, so a rotation never recreated the activity — which
            matters more than it looks: a recreated activity tears down the
            `MediaCodec` in the middle of an encode.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:screenOrientation="portrait"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
