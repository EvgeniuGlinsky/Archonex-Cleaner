<?xml version="1.0" encoding="utf-8"?>
<!-- The same as `drawable/launch_background.xml`, and the same in the dark
     theme deliberately.

     The template varies this one by system theme, which is right for a white
     splash and wrong for a coloured one: the icon does not change colour in the
     dark, so neither does the field it launches from. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />
</layer-list>
