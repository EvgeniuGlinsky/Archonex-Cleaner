<?xml version="1.0" encoding="utf-8"?>
<!-- The window the system shows while the process starts.

     One flat colour, and the same one the icon's own background is: the app
     opens from a tap on that icon, and any other colour here reads as a frame
     drawn around it for as long as the launch takes. Flutter's template puts
     white here, which is the loudest possible version of that.

     `@color/splash_background` is written by `tool/brand_assets.py` out of the
     artwork itself, so the two cannot drift apart. See docs/BRAND.md. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />
</layer-list>
